package de.ketrwu.discordtemperature.service

/**
 * A service that coordinates when temperature data should be received from the [TemperatureService] and send to the [BroadcastService]
 */
interface PublishingService